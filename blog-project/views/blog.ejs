<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stylish Form</title>
    <style>
    
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #212121;
            font-family: Arial, sans-serif;
        }

       
        .container {
            max-width: 400px;
            background: linear-gradient(145deg, #ffffff, #e4e6eb);
            border-radius: 40px;
            padding: 30px 40px;
            box-shadow: 0px 25px 30px rgba(0, 0, 0, 0.15);
            text-align: center;
        }

        
        .heading {
            font-size: 26px;
            color: #0e73c0;
            font-weight: 700;
            margin-bottom: 20px;
        }

        /* Form styling */
        .form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        /* Input field styling */
        .form .input {
            width: 100%;
            padding: 12px 20px;
            border: none;
            border-radius: 25px;
            background: #fff;
            box-shadow: 0px 10px 10px rgba(192, 224, 255, 0.3);
            font-size: 14px;
            color: #333;
            transition: all 0.3s ease;
        }

        .form .input::placeholder {
            color: #aaa;
        }

        .form .input:focus {
            outline: none;
            box-shadow: 0px 0px 10px rgba(30, 144, 255, 0.5);
        }

        /* Submit button styling */
        .form .submit-button {
            padding: 12px;
            width: 100%;
            font-weight: bold;
            color: #fff;
            background: linear-gradient(135deg, #0e73c0, #1ab5e1);
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.2s ease;
            box-shadow: 0px 15px 15px rgba(30, 144, 255, 0.2);
        }

        .form .submit-button:hover {
            transform: scale(1.05);
            box-shadow: 0px 20px 20px rgba(30, 144, 255, 0.3);
        }

        .form .submit-button:active {
            transform: scale(0.95);
            box-shadow: 0px 10px 10px rgba(30, 144, 255, 0.2);
        }
    </style>
</head>
<body>
    <div class="container">
        <h2 class="heading">Upload Your Image</h2>
        <form class="form" action="" method="post" enctype="multipart/form-data">
            <input class="input" type="file" name="image" required>
            <input class="input" type="text" name="title" placeholder="Title" required>
            <input class="input" type="text" name="description" placeholder="Description" required>
            <button class="submit-button" type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
